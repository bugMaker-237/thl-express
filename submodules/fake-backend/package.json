{
  "name": "fake-backend",
  "version": "0.0.1",
  "description": "Fake backend service to test front end apps",
  "main": "index.js",
  "scripts": {
    "prepare": "tsc index.ts",
    "start": "nodemon --watch './**/*.ts,*.json' --exec ts-node ./index.ts || exit 1"
  },
  "keywords": [
    "fake",
    "backend",
    "server"
  ],
  "author": "Etienne Yamsi",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.12.0"
  }
}
